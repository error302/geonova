# METARDU — Session Handoff Plan (2026-08-30)

> **Handoff doc.** Another agent can pick up from here. Repo: `C:\Users\user\Desktop\METARDU` (branch `master`, remote `https://github.com/error302/metardu`).
> Production: 8 Docker containers on Oracle VM, live at https://metardu.space. **User is in production — all fixes must be low-risk.**

## Status legend
- [x] done · [~] in progress · [ ] not started · [!] blocked

## Tasks

## 1. Sync with GitHub
- [x] `git pull` succeeded — branch is `main` (not master), clean, up to date with `origin/main`.

### 2. Terrain basemap does not work — `src/app/map/hooks/useMapBasemaps.ts`
- [x] Diagnosis: terrain layer used OpenTopoMap which rate-limits production traffic; fallback via `img.onerror` in `tileLoadFunction` never fired (OL handles tile errors internally); maxZoom 17 clamp caused 404s.
- [x] FIXED: Esri World_Topo_Map is now primary (maxZoom 19), removed fragile fallback chain.

### 3. Stakeout mode does not work
- [x] Diagnosis: (a) `toggleStakeout` returned SILENTLY when `hasFeature('gps_stakeout')` was false — button appeared dead for free-plan users; (b) stakeout never auto-enabled GPS tracking, so the HUD sat on "Waiting for GPS position...".
- [x] FIXED in `useMapInteractions.ts`: visible upgrade message on gate failure + auto-enable GPS on stakeout start. Math in `stakeout.ts` verified correct.

### 4. M-Pesa Till 3370347 not properly configured
- [x] Backend `verify-till/route.ts` is complete; UI IS wired via `src/components/pricing/MpesaCheckoutModal.tsx` (POSTs to `/api/payments/mpesa/verify-till`).
- [ ] Follow-up (not blocking): honour-system verification — codes are trusted, not checked against Safaricom. Consider adding real transaction verification; also set Till number via env instead of hardcode.

### 5. Collaboration feature audit
- [x] Architecture: legacy WS server (`collaborationServer.ts`, :3001 Docker container) + Yjs/WebRTC client path. UI uses Yjs path.
- [x] KEY FINDING: WebRTC mesh is OPT-IN — only created when `NEXT_PUBLIC_COLLAB_SIGNALING_URL` is set. `.env.production.example` does NOT set it (only legacy `NEXT_PUBLIC_WS_URL`), so in production cross-client presence/points/chat DO NOT sync (local IndexedDB only). **ACTION NEEDED ON VM**: deploy a self-hosted y-webrtc signaling server and set `NEXT_PUBLIC_COLLAB_SIGNALING_URL` (e.g. `wss://metardu.space/collab-signaling`), then rebuild the app image. Without this env var the collaboration feature is dead in prod by design (audit H-10 security fix).
- [x] FIXED `useCollaboration.ts`: `...callbacksRef.current` was spread at render time (ref mutation never re-renders), so onChat/onFeatureEdit/onFeatureDelete never reached consumers. Now stable call-time wrappers.

### 6. Surveyor workflow (collect → process documents)
- [x] Traced: `/field/collect` → `/api/survey-points` → `/api/sync` → documents (`/api/submission/*`, C22, deed plans). Core chain sound; no blocking bugs found.

### 7. NEW: Mobile map pan difficult
- [x] FIXED `MapGlobalStyles.tsx`: `touch-action: none` was only on `.ol-viewport canvas` — the viewport itself still let the browser interpret drags as scroll gestures, and overscroll (pull-to-refresh) swallowed drag events. Added `touch-action: none; overscroll-behavior: none` on `.ol-viewport`.

### 8. NEW: Remove selected/wrong point on map
- [x] FIXED: "Remove point" button added directly to the feature selection popup (previously delete was only via tool dock / Del key — unreachable on mobile). Wired in `useMapInit.ts` select handler + `renderPopup` in `MapClient.tsx`.

### 9. Suggested further improvements (user asked for ideas — NOT yet implemented)
- [ ] Deploy self-hosted y-webrtc signaling (see 5) — highest impact.
- [ ] Serve own terrain tiles from existing TIN/contour pipeline (python_worker) — removes third-party dependency.
- [ ] Real Safaricom transaction verification (ConfirmTicket API) for till payments.
- [ ] Undo/redo already exists (Ctrl+Z); consider adding an undo toast after "Remove point".
- [ ] Offline queue indicator on map for poor-field-connectivity.


## Commit strategy
- Commit per-area, small commits, descriptive messages. Final push only after tests pass (`npx tsc --noEmit` targeted files; run relevant unit tests).

## Remaining (as of this save)
1. Verify git pull actually succeeded (network was timing out).
2. Stakeout: pinpoint why `stakeoutActive` never activates (Pro+ gate vs wiring) and fix.
3. Checkout page wiring to till verify endpoint + env check.
4. Collaboration: confirm signaling server; write test for callbacks fix.
5. Mobile touch pan fix (task 7).
6. Point removal UI + API delete wiring (task 8).
7. Run type-check + targeted tests, commit everything.
